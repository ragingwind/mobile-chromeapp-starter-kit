{
  "private": true,
  "devDependencies": {
    "google-font-import": "^0.1.2",
    "maniver": "^0.2.0",
    "mkdirp": "^0.5.1",
    "shelljs": "^0.5.3"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "dependencies": {},
  "scripts": {
    "clean": "rm -rf package src/polymer-starter-kit/dist",
    "setup": "node bin/setup $NAME \"$PLATFORM\"",
    "build": "cd src/polymer-starter-kit; gulp",
    "packaging": "node bin/package.js build",
    "chrome": "cd platform; cca run chrome",
    "android": "cd platform; cca run android --emulator",
    "ios": "cd platform; cca run ios --emulator",
    "push": ""
  }
}
